import { Container } from "react-bootstrap"
import { RegistrationForm } from "../components/RegistrationForm"

export const Registration = () => {
  return (
    <Container className='mw-50 m-0 m-auto mt-5'>
      <RegistrationForm/>
    </Container>
  )
}